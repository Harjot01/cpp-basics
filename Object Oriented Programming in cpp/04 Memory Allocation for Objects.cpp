// Don't Overthink, Just Code
#include <bits/stdc++.h>
using namespace std;

/*
-> Member functions are created and placed in the memory space only once when they aer defined as a part of  a class specification.

-> Since all the objects belonging to that class use the same member functions, no separated space is        allocated for member functions when the objects are created. Only the space for member variables is allocated separately for each object

-> Separate memory locations for the objects are essential, because the member variables will hold different data values for different objects.
*/

int main()
{
    
    return 0;
}